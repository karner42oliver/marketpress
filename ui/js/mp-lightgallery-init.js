// Dummy für die Lightbox-Initialisierung, falls noch irgendwo aufgerufen
window.initProductGalleryLightbox = function(selector, options = {}) {
  // Keine Funktion mehr, da lightGallery entfernt wurde
  return null;
};
